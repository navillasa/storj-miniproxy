language: node_js
node_js:
  - "6"
dist: trusty
sudo: true
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
compiler:
  - clang
  - gcc
script:
  - npm install
  - npm run test
after_success: npm run coverage
